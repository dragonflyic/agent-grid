projects:
  - name: agent-grid-infra
    dir: infrastructure/terraform/environments/dev
    opentofu: true
    terraform: false
    workflow: default

workflows:
  default:
    plan:
      steps:
        - init
        - plan
    apply:
      steps:
        - init
        - apply
    on_commit_to_default:
      - apply
